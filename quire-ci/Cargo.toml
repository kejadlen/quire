[package]
name = "quire-ci"
version = "0.1.0"
edition = "2024"

[dependencies]
fs-err = { workspace = true }
miette = { workspace = true, features = ["fancy"] }
quire-core = { path = "../quire-core" }
